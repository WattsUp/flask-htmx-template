# Vale configuration for repository prose.
# Run `vale sync` after installing Vale to download the declared styles.
StylesPath = .vale/styles
MinAlertLevel = error
Packages = Microsoft, proselint, write-good
Vocab = Template

[*.{md,rst,adoc,txt}]
BasedOnStyles = Vale, Microsoft, proselint, write-good
